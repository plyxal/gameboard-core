{
  "debug": {
    "path": "config/debug.config",
    "config": {
      "enabled": true
    }
  },

  "restify": {
    "path": "config/restify.config",
    "config": {
      "name": "system-core",
      "version": "0.0.1",
      "port": 3000,
      "useCORS": true
    }
  },

  "sequelize": {
    "path": "config/sequelize.config",
    "config": {
      "database": "plyxal",
      "username": "plyxal",
      "password": "plyxal",
      "host": "127.0.0.1",
      "dialect": "postgres"
    }
  },

  "buttonDeamon": {
    "path": "daemon/button.daemon",
    "config": {
      "gpioId": 499
    }
  },

  "systemController": {
    "path": "controller/system.controller"
  },

  "userController": {
    "path": "controller/user.controller"
  },

  "gameController": {
    "path": "controller/game.controller"
  },

  "volumeController": {
    "path": "controller/volume.controller"
  },

  "brightnessController": {
    "path": "controller/brightness.controller"
  },

  "wifiController": {
    "path": "controller/wifi.controller"
  },

  "systemService": {
    "path": "service/system.service",
    "config": {
      "suspendResume": true,
      "killApp": true
    }
  },

  "userService": {
    "path": "service/user.service"
  },

  "gameService": {
    "path": "service/game.service"
  },

  "volumeService": {
    "path": "service/volume.service",
    "config": {}
  },

  "brightnessService": {
    "path": "service/brightness.service",
    "config": {
      "enabled": true
    }
  },

  "wifiService": {
    "path": "service/wifi.service",
    "config": {
      "debug": true
    }
  },

  "userDao": {
    "path": "dao/user.dao"
  },

  "gameDao": {
    "path": "dao/game.dao"
  },

  "userModel": {
    "path": "model/user.model",
    "config": {
      "table": "users"
    }
  },

  "gameModel": {
    "path": "model/game.model",
    "config": {
      "table": "games"
    }
  },

  "seed": {
    "path": "service/seed.service",
    "config": {
      "seedGames": false
    }
  }
}